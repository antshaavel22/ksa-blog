/**
 * Global blog display settings.
 * Change here → applies to every post and card instantly.
 * Per-post overrides: add `hideDate: true` or `hideAuthor: true` in frontmatter.
 */
export const BLOG_CONFIG = {
  showDate: true,    // set false to hide date everywhere
  showAuthor: true,  // set false to hide author everywhere
};
